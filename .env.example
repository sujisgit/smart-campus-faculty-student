# Frontend Environment Variables
# Set these before building/running the app

# Development (local)
NODE_ENV=development
# API will automatically use http://localhost:5000

# Production (Render)
# NODE_ENV=production
# RENDER_API_URL=https://your-render-app.onrender.com
